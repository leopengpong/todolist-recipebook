export class Ingredient {
  constructor(public name: string, public amount: number) {}
  // typescript's shortcut to make a plain object like this
  // see recipe.model for how to make object without shortcuts
}
